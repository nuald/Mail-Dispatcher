#!/usr/bin/env python 
import sys
from os.path import dirname, join, pardir
sys.path.insert(0, join(dirname(sys.argv[0]), pardir, 'share',
                        'maildispatcher', 'lib'))
from maildispatcher import main
main()

